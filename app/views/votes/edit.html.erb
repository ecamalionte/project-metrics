<%- model_class = Vote -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>

<%= render partial: 'modal_vote_form'%>

<script>
  $('#voteModal').modal({
    show:     true,
    keyboard: false
  });
</script>
